# packages/shared

Shared runtime code consumed by multiple applications (e.g. validation helpers, status
mappings, or a typed API client).

For Sprint 0 this package is intentionally empty. A shared OpenAPI client was explicitly
deferred to keep the foundation minimal; it may be introduced here in a later sprint if the
benefit outweighs the added complexity.
