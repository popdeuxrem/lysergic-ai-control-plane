# packages/types

Shared TypeScript type and interface definitions used across the frontend and any future
tooling.

For Sprint 0 the frontend defines its local component types inline. Cross-cutting types
(e.g. the `/health` contract) will be centralized here when a shared client is introduced.
